{
  "name": "ezee-money-tracker",
  "version": "1.0.1",
  "description": "A free personal money tracker software.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "money",
    "accounting",
    "money",
    "tracking"
  ],
  "scripts": {
    "package-win": "electron-packager . ezee-money-tracker --overwrite --asar=true --platform=win32 --arch=ia32 --icon=icon.ico",
	"package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=icon.icns --prune=true --out=release-builds"
  },
  "author": "Habibie",
  "license": "MIT",
  "devDependencies": {
    "electron": "^4.0.5"
  }
}
